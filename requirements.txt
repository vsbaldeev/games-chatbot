# CPU-only torch: the +cpu builds exist only on the PyTorch index and only
# for Linux; macOS wheels are CPU-only by nature and live on PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu; sys_platform == "linux"
torch==2.9.1; sys_platform == "darwin"
python-telegram-bot[job-queue]==22.8
langchain==1.4.0
langchain-groq==1.1.3
langchain-openai==1.6.2
langchain-core==1.6.3
langgraph==1.2.11
groq>=0.23.0
httpx>=0.28.0
beautifulsoup4>=4.12.0
python-dotenv>=1.1.0
asyncpg>=0.30.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
trafilatura>=1.6.0
duckduckgo-search>=8.0.0
tavily-python>=0.5.0
av>=12.0.0
numpy>=1.26.0
scipy>=1.11.0  # required by the Silero TTS model package at load time
Pillow>=10.0.0
fastembed>=0.4.0
pgvector>=0.3.0
# zoneinfo's IANA database is not guaranteed present on the slim Docker base;
# life_post.py schedules in Europe/Kyiv and must resolve DST correctly.
tzdata>=2024.1
